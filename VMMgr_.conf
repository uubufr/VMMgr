### Main Config for VMMgr_Launcher
#
# You must define these vars before doing anything with VMMgl_Launcher
#
# This is the 1rst environment loaded by the script, thus it ensure some vars have a default value.
# This is usefull if you want all VM shared some values, this make template more easy to define

### Must be defined only here ###
VMMGR_PATH=$(pwd)
VMMGR_LIB_PATH=$VMMGR_PATH
VMMGR_DATA_PATH=$VMMGR_PATH
VMMGR_CONFIG_PATH=$VMMGR_PATH
VMMGR_DB_PATH=$VMMGR_PATH
VMMGR_ISO_PATH=$VMMGR_PATH/ISO
TPLNULL=""

### Once installed you will have by default: ###
#VMMGR_PATH="/usr/local/bin"
#VMMGR_LIB_PATH="/usr/local/lib"
#VMMGR_DATA_PATH="/var/lib/VMMGR"
#VMMGR_CONFIG_PATH="/etc/VMMGR"
#VMMGR_DB_PATH="/var/lib/VMMGR/DB"
#VMMGR_ISO_PATH="/var/lib/VMMGR/ISO"

### May be overriden on Templates and vm configs files ###
IOTHREADS=1

# Default RAM
MEM=2
MAXMEM=2
SLOT=1

# Defaut CPU
CPU=HOST
MACHINETPL=MACHINE35
SMPTPL=MAX

### Default NETWORK ###
IPVER=4
MINPORT=2000
MAXPORT=3000


### SPICE ###
SPICEMODE=plaintext
SPICEPWD="spicepwd"
SPICEIPVER=$IPVER
SPICEPORT=new
SPICEMINPORT=$MINPORT
SPICEMAXPORT=$MAXPORT


### TELNET ###
TELNETIPVER=$IPVER
TELNETPORT=new
TELNETMINPORT=$(( $MINPORT + 1000 ))
TELNETMAXPORT=$(( $MAXPORT + 1000 ))

### VNC ###
VNCIPVER=$IPVER
VNCPORT=new
VNCMINPORT=5900
VNCMAXPORT=6900

### QMP ###
QMPIPVER=$IPVER
QMPPORT=new
QMPMINPORT=$(( $MINPORT + 2000 ))
QMPMAXPORT=$(( $MAXPORT + 2000 ))

export VMMGR_PATH VMMGR_LIB_PATH VMMGR_DATA_PATH VMMGR_CONFIG_PATH VMMGR_ISO_PATH VMMGR_DB_PATH VMMGR_PATH
